name: Portfolio OOS Backtest

on:
  workflow_dispatch:

jobs:
  backtest:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance xgboost lightgbm scikit-learn pandas numpy

      - name: Run portfolio backtest
        run: |
          python portfolio_backtest.py

      - name: Commit results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add portfolio_backtest_results

          git diff --cached --quiet || git commit -m "Add portfolio OOS backtest results"

          git push
